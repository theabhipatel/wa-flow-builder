IMPORTANT RULES:
- Do NOT create any new markdown (.md) files.
- Do NOT generate documentation files like README, CHANGELOG, NOTES, FIXES, docs/*.md.
- Only write code inside existing source files.
- If you need to explain something, explain inside chat in short, not as files.
- Only create new files if they are required for actual code (like .ts, .js, .json).
